"""
Chargement des sources brutes du corpus (data/raw/).

Ce module lira les fichiers sources bruts (ex: exports texte, PDF, notes
d'entretiens RH) déposés dans data/raw/ et les convertira en une structure
intermédiaire exploitable par clean.py, avant intégration dans
data/processed/corpus.jsonl.

Non implémenté pour l'instant : le corpus initial est saisi directement
dans data/processed/corpus.jsonl.
"""

# TODO: implémenter le chargement des sources brutes (data/raw/) une fois
# le format des documents sources (texte libre, PDF, CSV...) défini.
